name: gae-deployment

on:

  push:

    branches: [ 'service/*' ]

jobs:

  deploy:

    runs-on: ubuntu-latest

    steps:

      - name: Check out repository
        uses: actions/checkout@v2

      - name: Set up java environment
        uses: actions/setup-java@v2
        with:
          java-version: 11
          distribution: temurin
          cache: maven

      - name: Authenticate with GCP
        id: auth
        uses: google-github-actions/auth@v0
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Convert trigger service branch name into GAE version
        id: release
        run: echo ::set-output
          name=version::$(echo ${{ github.ref_name }} | sed -r 's:service/(.*):\1:')

      - name: Deploy GAE version
        run: >
          GOOGLE_APPLICATION_CREDENTIALS='${{ steps.auth.outputs.credentials_file_path }}'
          mvn --batch-mode --update-snapshots
          -Drevision=${{ steps.release.outputs.version }}
          deploy
